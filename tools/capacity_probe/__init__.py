# Package marker. The directory name is a legal Python identifier, so the
# modules here import as `tools.capacity_probe.<module>` from the repo root
# as well as by bare name in script-mode (probe.sh / with-proxy.sh, where
# Python auto-prepends the script's directory to sys.path).
